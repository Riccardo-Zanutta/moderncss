section
  section
    h3 Syntax
    .row
      .half
        p .sass
        pre
          code(data-trim).
            .foo
              color: black
            // indentation
      .half
        p .scss
        pre
          code(data-trim).
            .foo {
              color: black;
            }
  section
    h2 Compiled to:
    pre
      code(data-trim).
        .foo {
          color: black;
        }